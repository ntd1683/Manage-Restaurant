// Recommended way, to include only the icons you need.
import {createIcons, Menu, User, Search, ShoppingBag, Play, Sun, Moon, ChevronUp} from 'lucide';

createIcons({
    icons: {
        Menu,
        Search,
        ShoppingBag,
        Play,
        User,
        Sun,
        Moon,
        ChevronUp,
    }
});
